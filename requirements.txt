# Core ML Framework
torch>=2.0.0
# transformers not needed - implementing PatchTST from scratch or using a standalone implementation

# Data Processing
pandas>=2.0.0
pyarrow>=14.0.0
fastparquet>=2023.10.0

# Data Acquisition
yfinance>=0.2.40
fredapi>=0.5.0

# Technical Indicators
pandas-ta>=0.3.14b
# !!! TA-Lib - pip install requires compiled binary; install via: brew install ta-lib, then pip install TA-Lib

# Advanced Mathematical Features (Category 16 - Indicator Catalog v0.4)
# Fractal Analysis
antropy>=0.1.6           # Higuchi, Katz, Petrosian fractal dimensions
nolds>=0.6.0             # Lyapunov exponent, correlation dimension, DFA
MFDFA>=0.4.3             # Multifractal Detrended Fluctuation Analysis
hfda>=0.2.0              # Alternative Higuchi Fractal Dimension

# Spectral Decomposition
EMD-signal>=1.6.0        # EMD, EEMD, CEEMDAN (PyEMD)
PyWavelets>=1.4.0        # Wavelet decomposition

# Recurrence Quantification Analysis
PyRQA>=8.2.0             # RQA features (DET, LAM, entropy)

# Topological Data Analysis (optional - computationally heavy)
# giotto-tda>=0.6.0       # Persistent homology, Betti curves
# Uncomment when implementing TDA features - requires significant compute

# Cross-Correlation Analysis
# py-DCCA: Install from GitHub: pip install git+https://github.com/letfoolsdie/dcca.git

# Hyperparameter Optimization
optuna>=3.5.0
optuna-dashboard>=0.15.0

# Experiment Tracking
wandb>=0.16.0
mlflow>=2.10.0

# ML Utilities
scikit-learn>=1.4.0
numpy>=1.24.0
scipy>=1.11.0
# mapie - for conformal prediction; verify this is the preferred library
mapie>=0.8.0
# catboost - for phase 2 of the project - meta-module stacking
catboost>=1.2.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Development & Testing
pytest>=8.0.0
pytest-cov>=4.1.0
black>=24.0.0
ruff>=0.3.0
ipython>=8.20.0

# System Monitoring
psutil>=7.0.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Foundation Models (Phase: Decoder Investigation)
gluonts[torch]>=0.14.0,<0.16
huggingface-hub>=0.20.0
packaging>=21.0  # For version comparison in tests